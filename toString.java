package org.example;

public class toString {
    /*
    //Eteto
    @Override
    public String toString() {
        return String.format("%s (%d; %d)",
                isAktiv() ? "On" : "Off",
                pozicio.getX(), pozicio.getY());
    }
    //TapAdagolo
    @Override
    public String toString() {
        return String.format("TapAdagolo: %s, Formatum: %s",
                super.toString(), formatum);
    }

    //VitaminAdagolo
    @Override
    public String toString() {
        return String.format("VitaminAdagolo: %s, A:%d - F:%d",
                super.toString(), alsoHatar, felsoHatar);
    }
     */
}